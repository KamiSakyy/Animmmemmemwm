# Animmmemmemwm

Текущий продукт: **YORU Android Java 4.12.6** (`app.yoru.mobile`).

Исходники находятся в `yoru-android/app`. Kotlin-модуль не участвует в активной сборке; Gradle собирает только `:app`. Forge/toolchain в этой линии не используется.

Готовый release APK после CI: `apk-output/YORU-4.12.6-release.apk`.
SHA256 APK: `eadeaa777b22cde1507ccc4ed11176ba5c379b8546254bb22e3c8d93a0bc3ae8`.
Инструкция сборки: `handoff/BUILD_APK_GUIDE.ru.md`.

Ключ 4.12.6: hotfix после 4.12.5 — полностью убран стартовый загрузочный экран/надписи; нижняя кнопка всегда просто `Календарь` без цифр/бейджа; календарь снова показывает весь список событий по умолчанию через `Все · N`, а дни работают как быстрые фильтры.
