# YORU Android Java 4.12.1

YORU — стабильное Java-приложение `app.yoru.mobile` для поиска, карточек, просмотра, загрузок, коллекции и уведомлений о новых сериях. Текущий активный продукт — Java. Kotlin-модуль удалён из Gradle и из исходников ветки.

## Главное в 4.12.1

- Исправлено: будущие серии больше не показывают `Видео-превью`, если реально они ещё не вышли.
- Если расписание говорит `Вышло 15 из 25`, серии 16–25 становятся непроигрываемыми заглушками; первая показывает точный `nextEpisodeAt`, остальные — `Дата уточняется`.
- Future-серии очищаются от play/download-потоков и постеров, поэтому не выглядят как доступные.
- Календарь `Избранное` снова показывает добавленные тайтлы: YORU сопоставляет один тайтл между разными источниками по ID и названию.
- Календарь отдельно проверяет расписание избранных, а не ждёт, что они попадут в общий топ онгоингов.
- Календарь раскрыт до 21 дня и сам прыгает на ближайший день с событием по выбранному фильтру.
- Уведомления о новых сериях теперь сравнивают `episodesAired`/реально playable серии, а не плановое количество.
- Java-only сборка сохранена: активный модуль только `:app`, версия `4.12.1` / `44`.

## Сборка

```bash
cd yoru-android
./gradlew --no-daemon :app:assembleRelease
```

Готовый файл локально:

```text
yoru-android/app/build/outputs/apk/release/app-release.apk
```

В Arena-песочнице локального JDK может не быть. Ветка содержит workflow `.github/workflows/build-apk.yml`, который собирает release APK на GitHub Actions.

## Правила продолжения

- Не возвращать Kotlin без прямой команды пользователя.
- Не возвращать Clips, “Ещё”, AniList как пользовательский источник, TSM, AnimeGO, JutSu, SameBand, SovetRomantica, Yummy Legacy.
- Не добавлять VPN/VLESS/Xray/proxy/private token и MP4-конвертацию.
- “Озвучка” — только реальная команда перевода/дубляжа: AniDUB, AniLibria.TV, AniMaunt, AnimeVost, AniStar & DEEP, Beyond:Studio, Dream Cast, AniMedia и т.п.
- Скрытые маршруты можно использовать только под капотом и нельзя показывать их как озвучки.
