# YORU для Android

Нативное Android-приложение на Java, Android SDK и Gradle для просмотра аниме: каталог, поиск, карточки, коллекция, история, загрузки и настройки сделаны стандартными Android Views. Онлайн-видео открывается сразу: прямые потоки идут через Media3 ExoPlayer, YummyAnime iframe запускается без ожидания resolver, а извлечение прямого потока выполняется фоном.

## Требования

- Android 8.0 / API 26 и новее.
- Интернет для актуального каталога, просмотра и проверки новых серий.
- Актуальный Android System WebView для внешних iframe-плееров.
- Разрешение на уведомления на Android 13+ для фоновых загрузок и сообщений о новых сериях.

## Что включено

- Каталоги: Все источники, YummyAnime, AniLiberty, AnimeVost, AniLib, AniLib 4K probe, AniMedia, Animetka, AniDub, JutSu, Kodik 2026, Shikimori.
- Быстрый поиск с историей запросов, очисткой истории и моментальными локальными совпадениями из коллекции, истории просмотра и стартового каталога.
- AniList и SameBand убраны из приложения.
- В плеере доступен выбор «Все источники» или конкретного источника: YummyAnime, AniLiberty, AnimeVost, AniLib, AniMedia, Animetka, AniDub, JutSu, Kodik 2026.
- YummyAnime Iframe для серий, озвучек и студий, которые доступны только через внешний плеер.
- Нативный плеер Media3 для прямых MP4/HLS/DASH-потоков AniLiberty, AnimeVost, AniMedia, AniDub, JutSu и распознанных iframe-вариантов; для YummyAnime iframe при удачном извлечении открывается встроенный просмотр.
- Минималистичный видеоплеер: fullscreen, PiP, пауза/продолжить, −10/+10 и масштаб находятся прямо поверх видео компактными иконками. Fullscreen поворачивает просмотр в landscape и скрывает интерфейс приложения и системные панели.
- Принудительная подготовка скачивания из текущего YummyAnime iframe: resolver ловит Kodik, CVH/CdnVideoHub и реальные `.m3u8`, `.mpd`, `.mp4`, `.mkv`, `.webm` запросы во время воспроизведения без тяжёлых фоновых probe-задач.
- Скачанные одиночные видеофайлы можно открыть во внешнем плеере, отправить, сохранить в `Видео / YORU` или выбрать папку через системный проводник. HLS/DASH офлайн-загрузки больше не пересобираются в отдельный файл и открываются напрямую во встроенном плеере YORU.
- Офлайн-раздел показывает быстрые действия `YORU`, `Проводник` для одиночных видеофайлов, `Плеер`, `Удалить`, статус, формат и размер скачанной серии.
- Массовое скачивание из карточки аниме: следующие 3, 6, 12 серий или выбор нескольких серий из списка, затем общее качество.
- Скачивание собирает общий пул из текущего плеера, родного источника, YummyAnime, AniLiberty, AnimeVost, AniLib, AniMedia, Animetka, AniDub и Kodik 2026, если источники отдают доступный поток без пользовательских API-ключей.
- Подбор прямых потоков для загрузки из Kodik, Alloha, Aksor, Sibnet, Stormo, VK, Aniboom и AniLib, если плеер отдаёт доступный адрес.
- В карточке аниме скачивание выбирает серию из списка, а в плеере скачивает текущую серию без ручного ввода номера.
- Выбор серий, озвучек, студий, плееров и качества на экране просмотра и перед скачиванием.
- Коллекция со статусами, история просмотра, продолжение прямого видео и офлайн-просмотр завершённых загрузок.
- Уведомления о новых сериях для тайтлов в коллекции: проверка после добавления в избранное, ручная проверка в профиле, периодическая проверка после запуска приложения и после перезагрузки устройства.
- Импорт веб-коллекций YORU v1–v5 и коллекций приложения. Новые экспорты используют формат yoru.android.v2.

Доступность видео, реклама и ограничения региона определяются источниками. Аниме-источники не обходят авторизацию, проверки браузера или ограничения правообладателей. Если внешний плеер не отдаёт прямой поток, приложение показывает онлайн-вариант и не создаёт пустую загрузку.

## Офлайн и сеть

Раздел «Загрузки» показывает состояние сохранения. Офлайн-просмотр разрешён только для завершённых загрузок. Их данные находятся в приватном внутреннем каталоге приложения, отдельно от временного кэша. Очистка кэша не удаляет сохранённые серии.

По умолчанию загрузки ожидают Wi-Fi / безлимитную сеть. Переключатель находится в профиле. Для фоновых загрузок используется видимое уведомление; Android 13+ может запросить разрешение на уведомления. Поведение фоновой работы также зависит от ограничений Android и производителя устройства.

Счётчик трафика начинается после установки обновления / сброса. Разделение по типу сети является оценкой; при неотслеживаемых сменах сети часть расхода учитывается как «другой». Дополнительный доступ к статистике остальных приложений не запрашивается.

## YORU 3.1.2

- Удалён неработающий источник `Yummy Legacy 2026` из каталога, выбора плеера, поиска источников и вариантов скачивания.
- Добавлен отдельный источник `AniDub`: DLE-каталог/поиск, карточки, серии из AniDub-плееров и резолвинг реальных потоков через Sibnet HLS, Stormo MP4 и AniDub HLS.
- Добавлен отдельный источник `JutSu`: каталог популярных тайтлов, карточки, список серий и прямые MP4/HLS-потоки из HTML5-плеера.
- Новые источники не входят в режим `Все источники`, чтобы общий каталог и автопоиск не тормозили. Их можно выбрать отдельной вкладкой или конкретным плеером.
- Версия Android-пакета поднята до `versionName 3.1.2`, `versionCode 20`.

## YORU 3.1.1

- Убрана отдельная текстовая плашка из видеоплеера: поверх видео остались только компактные иконки управления.
- Добавлен отдельный источник `Animetka`: топ/поиск, карточки, франшиза, прямые AniLibria HLS и Kodik-варианты озвучек из публичного API Animetka.
- `Animetka` не включён в режим `Все источники`, чтобы новый источник не замедлял общий каталог. Его можно выбрать отдельной вкладкой или конкретным плеером.
- Для Animetka подтягиваются названия переводов через playlist API, если endpoint быстро отвечает; иначе показывается номер перевода.
- Версия Android-пакета поднята до `versionName 3.1.1`, `versionCode 19`.

## YORU 3.1.0

- Управление плеером перенесено внутрь видео: маленькие иконки fullscreen, PiP, пауза/продолжить, −10/+10 и масштаб.
- PiP получил системные кнопки Android для паузы/продолжения и перемотки.
- `Yummy Legacy 2026` удалён из приложения; `AniLib 4K probe` оставлен отдельной вкладкой без замедления режима `Все источники`.
- Резолвер потоков теперь не отбрасывает 1440/2060/2160/4320 и дополнительные 4K/UHD-поля, если источник реально их отдаёт.
- GitHub Actions ускорен кешем Gradle/Android SDK и собирает только release APK.

## YORU 3.0.0

- Поиск получил историю запросов, быстрые совпадения и защиту от лишних медленных запросов.
- `Все источники` быстрее отдаёт первые результаты и не ждёт тормозящие источники до бесконечности.
- `Полный экран` теперь даёт чистое видео без интерфейса YORU, с автоповоротом в landscape и скрытием системных панелей.
- Добавлена кнопка `PiP` и автоматический переход в картинку-в-картинке при уходе домой во время просмотра.
- GitHub Actions собирает только release APK без debug APK.

## YORU 2.9.0

- VPN полностью удалён: нет нижней вкладки, `VpnService`, Xray/libv2ray и скачивания тяжёлого AAR при сборке.
- Раздел загрузок больше не запускает пересборку HLS/DASH в отдельный видеофайл, поэтому вход в `Загрузки` не должен греть телефон и падать из-за конвертации.
- HLS/DASH офлайн открывается напрямую во встроенном плеере YORU.
- Плеер получил tap-to-fullscreen, долгое нажатие для масштаба и ±10 секунд.

## Сборка

Откройте эту папку в Android Studio либо используйте Gradle Wrapper.

1. Установите JDK 17 или новее.
2. Установите Android SDK Platform 36.
3. Укажите путь SDK в `local.properties` (`sdk.dir=...`) или в `ANDROID_HOME`.
4. Сохраните приватную папку `owner-signing/` рядом с проектом, если собираете релиз с прежней подписью.
5. Выполните:

```sh
./gradlew --no-daemon :app:assembleRelease
```

Windows:

```bat
gradlew.bat --no-daemon :app:assembleRelease
```

Результат: `app/build/outputs/apk/release/app-release.apk`.

Проект использует Android Gradle Plugin 8.13.2, Gradle 8.13, Media3 ExoPlayer и jsoup. Приложение не требует собственного сервера и не включает весь сайт в WebView.

## Структура

- `app/src/main/java/app/yoru/mobile/` — код приложения.
- `ApiRepository.java` — публичные источники, сопоставление тайтлов, серии и варианты загрузки.
- `VideoResolver.java` — распознавание прямых потоков из iframe-плееров, включая Kodik 2026, CVH/CdnVideoHub и высокие 1440/2060/2160/4320 варианты там, где источник их отдаёт.
- `EpisodeUpdateReceiver.java` — проверка новых серий и уведомления.
- `MainActivity.java` — главная, каталог, коллекция и источники.
- `DetailsActivity.java` — описание и связанные истории.
- `PlayerActivity.java` — нативный проигрыватель и внешние плееры.
- `OfflineExporter.java` — экспорт скачанных одиночных видеофайлов во внешние плееры, системный проводник и `Видео / YORU`; HLS/DASH не пересобирается.
- `DownloadHub.java` — очередь загрузок Media3 и офлайн-воспроизведение.
- `SecureStore.java` — локальные данные, история поиска и настройки, шифруемые ключом Android Keystore.
- `AppSecurity.java` — проверка релизной подписи.
- `app/src/main/assets/` — стартовая подборка и обложки для начального/офлайн-экрана.
- `owner-signing/` — приватные материалы подписи владельца.
- `SECURITY.ru.md` — модель защиты и её ограничения.

## Обновления

Для обновления установленного приложения нужна та же подпись. Сохраняйте ключ из `owner-signing/`. При замене ключа обновите отпечаток `certificate-sha256.txt`; приложения, подписанные разными ключами, нельзя установить друг поверх друга без удаления прежнего.

Увеличивайте `versionCode` в `app/build.gradle` при выпуске новых APK.

## Важно владельцу

Не публикуйте архив исходников целиком в открытом репозитории, если в нём остаётся `owner-signing/`. Перед публикацией кода удалите приватные материалы подписи и любые локальные настройки. Этот README предназначен владельцу проекта.
