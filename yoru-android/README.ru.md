# YORU для Android

YORU — закрытое Android-приложение для аниме: поиск, единый каталог, карточки, серии, коллекция, история, загрузки, уведомления о новых сериях, календарь выхода, настройки внешнего вида и удобный встроенный просмотр. Интерфейс организован по озвучкам и не заставляет пользователя выбирать технические детали.

## Требования

- Android 8.0 и новее.
- Интернет для актуальной подборки, просмотра и проверки новых серий.
- Разрешение на уведомления на Android 13+ для фоновых загрузок и сообщений о новых сериях.

## Что включено

- Единый каталог YORU Source: карточки не дробятся по техническим направлениям, а просмотр и загрузки организованы по озвучкам.
- YORU Source автоматически подбирает рабочий маршрут; резервные маршруты дополняют его без лишних экранов.
- Быстрый поиск, история запросов, локальные совпадения, жанры, заметки, теги и умные фильтры через общий режим.
- Карточка аниме сразу показывает доступные серии, текущий прогресс, скачивание рядом с сериями и быстрый пакет продолжения.
- Встроенный просмотр: fullscreen, PiP, пауза/продолжить, −10/+10, предыдущая/следующая серия, масштаб, скорость просмотра, жесты яркости/громкости, пропуск начала и компактные иконки поверх видео без дублирующих панелей.
- Тихая подготовка следующей серии во время просмотра, если сеть не в экономии: переход к продолжению становится быстрее без новых кнопок.
- Автоспасение просмотра: если текущий вариант не открылся, приложение пробует другой рабочий вариант без лишней навигации.
- Скачивание серий с фильтрами по озвучке и разрешению, примерный вес перед сохранением, быстрый пакет «Скачать дальше», массовая очередь, пауза/продолжение всех загрузок, повтор ошибок и офлайн-просмотр прямо в YORU.
- Коллекция со статусами, история просмотра, план просмотра, заметки, теги, оценки, закрепления, пересмотры и скрытые тайтлы.
- Уведомления о новых сериях для тайтлов в коллекции: проверка запускается при добавлении в коллекцию, при запуске приложения, при возврате в приложение и частым фоновым циклом.
- Настройки комфорта: выбранная озвучка, режим «показывать только выбранную озвучку», разрешение для скачивания, режим без спойлеров, скрытие просмотренного, оригинальные названия, мини-режим интерфейса, таймер сна.
- Импорт и экспорт коллекции, выбор собственного `.ttf/.otf` шрифта через системный проводник.

Доступность просмотра и ограничения региона могут меняться. YORU не обходит авторизацию, проверки браузера или ограничения правообладателей.

## Офлайн и сеть

Раздел «Загрузки» показывает состояние сохранения. Офлайн-просмотр разрешён только для завершённых загрузок. Их данные находятся в приватном внутреннем каталоге приложения, отдельно от временного кэша. Очистка кэша не удаляет сохранённые серии.

По умолчанию загрузки ожидают Wi-Fi. Переключатель находится в настройках. Для фоновых загрузок используется видимое уведомление; Android 13+ может запросить разрешение на уведомления.


## YORU Kotlin 1.1.0

- Kotlin-приложение стало основным направлением разработки: быстрый нативный Media3-плеер сохранён как главный путь.
- Перенесён пользовательский дизайн YORU: тёмная фиолетовая тема, главный экран, нижняя навигация, коллекция, история, загрузки, календарь, настройки и карточки.
- Карточка теперь следует порядку YORU: описание, скриншоты с открытием на весь экран и приближением, трейлер, просмотр, серии, связанные тайтлы и похожее.
- Серии показывают превью, реальную озвучку, длительность, доступные качества `360p/480p/720p/1080p` и примерный размер.
- Добавлено локальное хранилище Kotlin: избранное, история просмотра, настройки, скачанные серии и экспорт данных.
- Добавлено сохранение выбранной серии в приватный офлайн-пакет для просмотра без сети, с уважением настройки Wi‑Fi-only.
- Плеер получил прогресс, сохранение позиции, авто-переход к следующей серии, пропуск начала, fullscreen, PiP, smart-fill, скорость до `2.5×`, двойной тап `−10/+10` и один чистый overlay.
- Workflow теперь собирает только Kotlin release APK, чтобы не тратить время на Java rebuild.
- Kotlin APK: `YORU Kotlin 1.1.0`, `versionCode 3`.

## YORU 4.10.0

- В основном плеере загрузка стала круглым индикатором без надписей.
- Добавлено отдельное экспериментальное приложение `YORU Kotlin`, полностью написанное на Kotlin.
- Kotlin-версия использует тяжёлый современный стек: Jetpack Compose Material 3, Navigation, ViewModel, Coroutines, OkHttp, Coil и Media3/ExoPlayer.
- `YORU Kotlin` устанавливается рядом с обычным YORU и нужен для сравнения скорости/ощущений.
- В Kotlin-версии есть каталог, поиск, карточка, серии, выбор качества, fullscreen и smart-fill.
- Основной APK: `versionName 4.10.0`, `versionCode 41`.
- Kotlin APK: `YORU Kotlin 1.0.0`.

## YORU 4.9.0

- YORU Player быстрее стартует: выбранная озвучка резолвится заранее, буфер старта стал меньше, плохие маршруты отсекаются быстрее.
- Старая надпись подготовки была заменена на компактный индикатор.
- Кнопки в плеере стали современнее: glass-стиль, акцентный play/pause, красивый `+10/−10` при быстрой перемотке.
- Добавлена кнопка `Умно расширить экран`: fullscreen + умное заполнение кадра без геометрического растягивания.
- Скорость просмотра теперь быстрее переключается и поддерживает режимы до `2.5×`.
- Shikimori работает через несколько зеркал для кадров, трейлеров, связанных аниме и расписания.
- Версия Android-пакета: `versionName 4.9.0`, `versionCode 40`.

## YORU 4.8.0

- Удалена короткая вертикальная лента: она больше не тратит интернет и не присутствует в приложении.
- Карточка показывает полный список серий, без лимита 80.
- Новый порядок карточки: описание → скриншоты → трейлер → просмотр → серии.
- Скриншоты открываются на весь экран с приближением двумя пальцами.
- Связанные аниме и фильмы добираются через Shikimori и сортируются по дате/году выхода.
- Плеер и иконки стали крупнее и аккуратнее.
- Перед первым запуском можно выбрать озвучку и разрешение, включая 360p; повторного вопроса при выбранной озвучке нет.
- Версия Android-пакета: `versionName 4.8.0`, `versionCode 39`.

## YORU 4.7.0

- Серия открывается без повторного вопроса, если озвучка уже выбрана.
- В плеере вместо пустой фразы показываются следующие серии этого же аниме.
- В списке серий появились превью видео: кадр серии, скриншот тайтла или постер как fallback.
- В описании аниме появился блок кадров и трейлера через Shikimori и доступные источники.
- Версия Android-пакета: `versionName 4.7.0`, `versionCode 38`.

## YORU 4.6.0

- Выбранная озвучка теперь реально блокирует остальные варианты в плеере: YORU не тратит трафик на чужие озвучки перед стартом.
- Новый скрытый резерв YORU Reserve добавляет больше серий и HLS-вариантов через открытое зеркало без авторизации.
- Новый резерв не показывает технические маршруты как озвучки и не замедляет основной YORU Source.
- Fallback остаётся внутри текущей озвучки, если она выбрана пользователем.
- Версия Android-пакета поднята до `versionName 4.6.0`, `versionCode 37`.

## YORU 4.5.2

- Перед просмотром серии можно выбрать озвучку из списка реальных озвучек, найденных именно у этой серии.
- Текущая озвучка по умолчанию видна на экране тайтла и на кнопках скачивания.
- Кнопка скачивания в плеере показывает текущую озвучку и не заставляет угадывать, какая дорожка будет сохранена.
- Если серия не передала название озвучки, YORU честно пишет `Дорожка без названия` и не подставляет внутренний маршрут.
- Версия Android-пакета поднята до `versionName 4.5.2`, `versionCode 36`.

## YORU 4.5.1

- Исправлена логика озвучек: YORU не показывает внутренние маршруты как озвучки.
- Плеер и скачивание показывают реальные доступные озвучки конкретной серии: например AniDUB, AniLibria.TV, AniMaunt, AnimeVost, AniStar & DEEP, Beyond:Studio, Dream Cast и другие, если они есть у тайтла.
- Фильтр «Показывать только выбранную озвучку» скрывает остальные озвучки и не подставляет технические названия.
- Перед скачиванием YORU загружает список доступных озвучек именно для выбранной серии и только потом ищет нужное разрешение.
- Добавлен handoff-пакет для восстановления проекта и сборки APK в будущей сессии.
- Версия Android-пакета поднята до `versionName 4.5.1`, `versionCode 35`.

## YORU 4.5.0

- Просмотр и скачивание переведены на озвучки: пользователь выбирает озвучку, а YORU скрыто подбирает рабочий маршрут.
- В настройках добавлен режим «Показывать только выбранную озвучку» для реальных озвучек: AniDUB, AniLibria.TV, AniMaunt, AnimeVost, AniStar & DEEP, Beyond:Studio, Dream Cast, AniMedia и других доступных названий.
- Окно скачивания заранее спрашивает озвучку и разрешение, поэтому YORU не перебирает лишние варианты.
- Массовое скачивание использует те же фильтры озвучки и разрешения.
- Одинаковые озвучки не дублируются в списке; запасные маршруты остаются внутри YORU Source.
- При сбое воспроизведения YORU Player пробует другой рабочий вариант без показа технических деталей.
- Версия Android-пакета поднята до `versionName 4.5.0`, `versionCode 34`.

## YORU 4.4.1

- Убран дублирующий стандартный контроллер видеоплеера: в видео остаётся один понятный слой кнопок YORU.
- Настройка скачивания снова показывает конкретные разрешения: 480p, 720p, 1080p.
- В настройках и загрузках оставлена формулировка только про Wi‑Fi.
- Главный экран и настройки сохраняют позицию прокрутки при выходе из приложения, переходе в другое приложение и возврате.
- Перед скачиванием и в плеере показывается быстрый ориентир размера серии по выбранному разрешению.
- Версия Android-пакета поднята до `versionName 4.4.1`, `versionCode 33`.

## YORU 4.4.0

- Полностью удалены два нерабочих направления из каталогов, автоподбора, просмотра, загрузок, изображений и документации.
- Нижняя навигация сокращена до пяти основных разделов; настройки остаются сверху и не занимают постоянную вкладку.
- Удалено откладывание уведомлений по расписанию: новые серии больше не блокируются этим фильтром.
- Удалён лишний безопасный переключатель из интерфейса, импорта и логики подбора.
- Проверка новых серий стала активнее: при добавлении в коллекцию, запуске YORU, возврате в приложение и частом фоновом цикле.
- Уведомление о новой серии получило быстрое действие «Смотреть».
- Список избранного для проверки расширен, чтобы больше тайтлов успевали обновляться за один цикл.
- Профиль переписан как компактные настройки: новые серии, сеть, стабильность загрузок, комфорт, память и приложение.
- Главный экран получил быстрый старт: продолжить просмотр, случайный тайтл, проверка новых серий и переход к загрузкам.
- Нативный плеер стал компактнее: предыдущая/следующая серия теперь внутри видео, больших кнопок под видео больше нет.
- Добавлена сохраняемая скорость просмотра.
- Добавлены жесты яркости и громкости в плеере.
- Улучшено возобновление: почти досмотренная серия не открывается заново на последних секундах.
- YORU Source быстрее отбрасывает слабые варианты, сильнее учитывает успешные запуски и меньше ждёт пустые направления.
- Увеличено полезное кеширование карточек и вариантов, чтобы повторное открытие работало быстрее.
- Редкие технические подписи вариантов скрываются за понятным запуском YORU.
- Долгое нажатие на серию в карточке отмечает её просмотренной.
- Версия Android-пакета поднята до `versionName 4.4.0`, `versionCode 32`.
