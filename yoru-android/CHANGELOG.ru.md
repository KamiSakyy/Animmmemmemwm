# YORU 4.26.0 — 2026-09-15

- Открытая карточка получает собственные потоки: список серий и описание загружаются отдельным пулом (три потока), поэтому не стоят в очереди за лентой, каталогом и поиском. Пул закрывается вместе с экраном.

Кэш, палитра, подпись и идентификатор пакета не менялись. Проверок на устройстве не было.

---

# YORU 4.25.0 — 2026-09-15

- В «Загрузках» появились общие действия: «Пауза всех», «Продолжить всех», «Повторить неудачные», «Убрать готовые». Удаление готовых запрашивает подтверждение и не трогает коллекцию и историю.

Кэш, палитра, подпись и идентификатор пакета не менялись. Проверок на устройстве не было.

---

# YORU 4.24.0 — 2026-09-15

- Быстрее открывается карточка и список серий: при поиске играбельного источника и при подготовке вариантов скачивания больше не запрашиваются связи франшизы, скриншоты и трейлеры — они загружаются только для самой карточки. Кэш при этом не теряет уже сохранённые связи: они переносятся из существующей записи.
- Будущие загрузки тоже используют облегчённый запрос метаданных.

Кэш, его лимиты и алгоритмы не менялись; палитра, подпись и идентификатор пакета прежние. Проверок на устройстве не было.

---

# YORU 4.23.0 — 2026-09-15

- Загрузки показывают скорость и остаток времени: строка размера дополняется «КБ/с» и «осталось N мин» для активного скачивания, расчёт сглаженный, без опросов сети.
- Иконка настроек перерисована: минималистичная шестерёнка вместо ползунков, геометрия вписана в ту же сетку 24 dp.

Кэш, палитра, подпись и идентификатор пакета не менялись. Проверок на устройстве не было.

---

# YORU 4.22.0 — 2026-09-15

- Список вариантов скачивания показывает настоящий размер сразу в строке: после открытия диалога размеры считаются параллельно (до четырёх потоков, первые восемь вариантов) и подставляются в строки без закрытия окна. Неизвестный размер не заменяется оценкой.
- Массовое скачивание готовит серии параллельно с общим прогрессом и отменой.
- Точный размер HLS по сегментам для скачивания, пакетной подготовки и будущих заданий; просмотр запускается без ожидания расчёта.

Кэш, палитра, подпись и идентификатор пакета не менялись. Проверок на устройстве не было.

---

# YORU 4.21.0 — 2026-09-15

- Точный размер HLS-потока: перед скачиванием и пакетной подготовкой размер считается по реальным сегментам плейлиста с учётом EXT-X-BYTERANGE и EXT-X-MAP, параллельными HEAD-запросами и общим таймаутом. Если источник не сообщает длину хотя бы одного сегмента, размер остаётся неизвестным, а не приблизительным.
- Просмотр не ждёт этот расчёт: видео запускается сразу по быстрой проверке формата, точный размер подставляется в фоне для текущего потока.
- Отмена расчёта размера связана с задачей экрана: при уходе с карточки, смене серии/озвучки или закрытии плеера все запросы сегментов снимаются.
- Массовое скачивание готовит серии параллельно (до шести потоков) с сохранением порядка, общим прогрессом и отменой;
  расчёт точного размера выполняется внутри той же подготовки и не блокирует открытие серии.
- Заголовок главной «Ваше следующее любимое аниме» переименован в «Рекомендации».
- Убрано верхнее действие «История» в экране коллекции; история по-прежнему доступна с главной.

Кэш, его лимиты и алгоритмы, палитра, звезда рейтинга, подпись и идентификатор пакета не менялись. Минификация, удаление ресурсов и ZIP-сжатие APK отключены. Тесты, провайдерские и проверки на устройстве не выполнялись.

---

# YORU 4.19.1 — 2026-09-09

- Исправлен порядок общего расписания, резервного запроса и избранного; добавлен REST fallback существующего Shikimori. Личные события не блокируют загрузку общих.
- Календарь при входе показывает все фильтры/дни по умолчанию и все полученные события без первой пятёрки. Частичный личный fallback обозначается явно.
- Большая кнопка будущих загрузок заменена обычными карточками запланированных серий в общем списке. Есть отмена; нет дублей после перехода к реальной загрузке.
- Список загрузок и заданий читается фоном; старые результаты после закрытия экрана не применяются.
- Добавлены регрессионные unit tests на общий feed, сбои/избранное, отображение полного списка и дедупликацию будущих карточек.
- Кэш не изменялся. Сторонние функции и облачные маршруты не внедрялись.

---

# YORU 4.19.0 — 2026-09-09

Основа: стабильный source ZIP 4.17.2. VersionCode 69, чтобы обновление устанавливалось и поверх 4.18.1.

- Кэш сохранён без изменений; RAM-кэш не добавлен. В ImageLoader изменена только политика отказа очереди, без изменений decode/storage/limits.
- Очереди больше не исполняют сетевые задачи на вызывающем UI-потоке при перегрузке. Отменённые Future удаляются из очередей; новые задания при перегрузке отклоняются с сообщением, а не вытесняют произвольные активные операции.
- Отмена задач при смене эпизода/экрана и закрытии диалогов скачивания; проверки прерывания перед сетевыми попытками. Незавершённые сетевые операции завершаются с учётом прежних timeout.
- Переиспользование ViewHolder календаря/серий; DiffUtil для частичных изменений. Повторное использование строк загрузок. Высота списка серий без двойного dp.
- Будущие скачивания: отдельная постоянная SQLite-очередь заданий, выбор озвучки/качества, фоновые проверки JobScheduler, восстановление расписания после перезагрузки, удаление планов через «Загрузки → Будущие скачивания».
- Выбранная серия и качество проверяются точно; выбранная озвучка не подменяется fallback-вариантом. Проверяется наличие потока, а не только дата календаря. Уже переданные загрузки управляются в обычном списке.
- Удалены «Новые серии» и «Только встроенный просмотр» из настроек; просмотр серий только в нативном плеере YORU, без создания WebView. Экспорт готовых файлов через проводник сохранён.

Проверки фона Android могут задерживаться в Doze, без сети, при ограничении батареи/фонового запуска, после force-stop. Точный запуск в минуту премьеры не гарантируется. Доступность озвучки/разрешения зависит от источника. Ручные проверки на физических устройствах остаются необходимыми.

---

# 4.15.0

- Версия Android-пакета поднята до `versionName 4.15.0`, `versionCode 57`.
- Убран заголовок `Подробнее`: расширенный блок теперь называется `Подробнее` и не раскрывает технические детали.
- Из карточек, плеера, календаря и настроек убраны технические фразы про ответы каталога, скрытый просмотр, авто-подбор и порционную подгрузку.
- Настройки очищены: удалены кнопка ручной проверки избранного, длинное техническое описание фоновых проверок, `Мини-режим интерфейса`, `Не сохранять новую историю`, пункт `Данные и чистка`, импорт/экспорт/чистка коллекции из пользовательского меню.
- Каталог снова открывается мгновенно: при reset сначала показывается локальный seed/быстрые совпадения, сеть только обновляет список без пустого экрана `Загружаем…`.
- Карточка тайтла грузит серии быстрее: скрытый просмотр запускается параллельно с полной карточкой, поэтому список серий может появиться до завершения всех метаданных.
- Hidden playback pipeline ускорен без изменения auto-mode: при найденной любимой озвучке или достаточном наборе серий YORU не ждёт все фоновые маршруты до таймаута.
- ImageLoader ускорен и улучшен визуально: убран `CopyOnWriteArrayList`, увеличен memory cache, включён `CallerRunsPolicy`, добавлен безопасный `inBitmap` reuse, сетевые картинки декодируются в `ARGB_8888` без принудительного `RGB_565` и без искусственного downsample до 700px.
- Disk-cache обложек сохраняет картинку без webp-82 пересжатия; кандидаты URL сначала пробуют более качественные варианты.
- YoruCache получил WAL и фоновый trim примерно раз в час вместо частой синхронной очистки.
- Старт ускорен: YoruShield больше не читает SecureStore в конструкторе ApiRepository на main thread, профиль маршрутов подтягивается асинхронно.
- Thread pools больше не используют `DiscardOldestPolicy`; foreground UI-worker расширен, чтобы каталог/детали/офлайн-индекс не стояли в одной длинной очереди.
- PlayerActivity сохраняет прогресс не каждую секунду, а throttled раз в ~7 секунд и по важным событиям; prewarm следующей серии ограничен.
- WebView освобождается при переходе на native playback, чтобы не держать лишнюю память.
- Счётчик трафика стал точнее и быстрее: восстановление прошлых счётчиков, стартовый sample без долгой паузы, обновление примерно каждые 1.5 секунды, persist вынесен из горячего UI-пути.
- Календарь стал cache-first: свежий кэш не запускает тяжёлое сетевое обновление при каждом открытии, UI больше не показывает технический текст про порции.
- Auto-mode по озвучкам сохранён; новые источники не добавлялись; функциональность просмотра/коллекции/папок/загрузок/уведомлений не урезалась.

# 4.14.0

- Версия Android-пакета поднята до `versionName 4.14.0`, `versionCode 56`.
- Auto-mode не менялся: YORU остаётся пользовательски ориентированным на озвучки, а не на ручной выбор скрытого просмотра.
- Пользовательские экраны больше не показывают техническое место хранения тайтла как отдельную сущность: карточки, детали и экспорт загрузки говорят про YORU/озвучки/дорожки.
- Карточки стали информативнее без новых кнопок: бейдж теперь показывает прогресс, коллекцию, онгоинг или вышедшие серии вместо технического названия места хранения.
- На карточках добавлен компактный индикатор прогресса просмотра и чип любимых озвучек/режима авто.
- Главная стала чище: блок “Сейчас в YORU” заменил перегруженный быстрый старт, оставлена только основная кнопка продолжения просмотра при наличии истории.
- Добавлен блок “Новые серии для вас” из коллекции/истории: он показывает тайтлы, где появились непосмотренные серии, без дополнительных лишних кнопок.
- Расширенная карточка убрала техническую строку места хранения и подчёркивает озвучки, Quality+ и скрытый авто-подбор по просмотре YORU.
- Экспорт/детали скачанного видео больше не выводят внутренний каталог; вместо этого показывают озвучку или авто-подбор YORU.
- 4.13.0 сохранён: Quality+, любимые озвучки, папки, Animetka playlist, фоновые уведомления и performance-правки не урезались.

# 4.13.0

- Версия Android-пакета поднята до `versionName 4.13.0`, `versionCode 55`.
- Большой релиз YORU Quality+: выбор качества расширен до 1440p/2160p там, где источник реально отдаёт такие потоки; апскейл не обещается и не подменяется маркетингом.
- Любимые озвучки стали списком приоритета: выбранные дорожки запоминаются, сортируют варианты в карточке/плеере/скачивании и не превращают авто-подбор в тупик.
- Строгий режим “только выбранная озвучка” сохранён отдельно для тех случаев, когда нужен именно один дубляж.
- Расширенная карточка показывает больше контекста: источник, студию, страну, премьеру, длительность, число переводов, рейтинги, Shikimori/KP ID, франшизу, медиа, папки и любимые озвучки.
- Animetka усилена через публичный playlist API `/api/anime/playlist?material=…&tid=…&episode=…`: YORU резолвит реальные качества из `file`-manifest, включая 1440/2160 только если они есть в ответе.
- Animetka добавлена в общий YORU-каталог как реально работающий публичный источник; пагинация top/search не обрезается искусственно.
- Коллекция получила папки/полки поверх старых buckets: один тайтл может оставаться в `Смотрю` и одновременно быть в пользовательских папках.
- Импорт/экспорт сохраняет новые `favoriteVoices`, `libraryFolders`, `folders` у избранного и расширенные поля карточки.
- Сохранены 4.12.11 Calendar `Все` с быстрым ленивым списком, фоновые уведомления 4.12.8 и performance-правки 4.12.9–4.12.10.

# 4.12.11

- Версия Android-пакета поднята до `versionName 4.12.11`, `versionCode 54`.
- Главный фокус: оптимизация календаря во вкладке/режиме `Все` без урезания данных и функций.

## 20 новых функций/улучшений 4.12.11

1. `Все` в календаре стартует только с первых 5 событий, чтобы не создавать тяжёлый первичный render.
2. При прокрутке вниз календарь добавляет следующие 5 событий через `notifyItemRangeInserted`, без полного пересоздания списка.
3. Добавлена кнопка `Ещё 5` внизу списка для ручной догрузки, если экран высокий и прокрутки пока нет.
4. Добавлен footer-прогресс `показано X из Y`, чтобы понятно было, сколько событий ещё скрыто.
5. Заголовок календаря теперь показывает `показано X из Y` именно для режима `Все`.
6. RecyclerView получил `DefaultItemAnimator` с короткими add/move/change animation — новые карточки появляются плавнее.
7. Чтение SQLite schedule-cache и legacy `calendarCache` перенесено в background executor, чтобы вкладка календаря открывалась без UI-паузы.
8. `quickShow()` больше не парсит кэш на UI-потоке; если данных нет, запускается обычная фоновая загрузка.
9. Персональные признаки `Избранное`/`Новые` не заставляют UI ждать `SecureStore`: если store ещё не прогрет, он догружается в фоне и календарь обновляется после готовности.
10. Внутри построения buckets добавлен memo favorite/history по anime key — повторяющиеся события не дёргают `SecureStore` заново.
11. Дубликаты событий от разных источников убираются по `anime/time/episode/kind`, чтобы список не раздувался.
12. Счётчики фильтров теперь кэшируются в `filterCounts`, а не пересчитываются сортировкой списка каждый раз.
13. Чипы фильтров показывают количество событий: `Все · N`, `Избранное · N`, `Скоро · N` и т.д.
14. Чипы дней показывают количество через прямой bucket-size, без лишнего `dayItems().sort()` для каждого дня.
15. Добавлена кнопка `К ближайшей` — быстрый переход к ближайшему будущему событию.
16. Если ближайшее событие дальше пятой позиции, календарь раскрывает нужное количество страниц и плавно скроллит к нему.
17. Карточки календаря получили цветовой акцент: премьеры, уже вышедшие, ongoing и обычные события визуально различаются.
18. В строке события добавлен мягкий suffix `в коллекции` или год, когда это доступно без тяжёлой загрузки.
19. Долгое нажатие на событие открывает добавление/перенос тайтла в коллекцию прямо из календаря.
20. Empty-state стал полезнее: для пустого календаря есть `Обновить`, для пустого фильтра — `Показать все`.

- Сохранены все UI/feature-правки 4.12.7–4.12.10: красивый home hero, полный календарь по всем дням, скрытые scrollbars, downloads previews, реальные озвучки, bounded cache/executors и фоновые уведомления.

# 4.12.10

- Версия Android-пакета поднята до `versionName 4.12.10`, `versionCode 53`.
- Cold start облегчен: `SecureStore` не грузит AndroidKeyStore в конструкторе, а preload запускается в фоне сразу после создания приложения.
- `afterFirstFrame()` больше не вызывает `store.lastEpisodeCheckAt()` на главном потоке; планирование фоновых проверок ушло в background executor.
- `warmStartup()` больше не парсит расписание/SQLite на UI: `todayScheduleCount()`, `api.seed()`, календарный refresh и cache trim выполняются в фоне.
- `YoruCache` перестал блокировать все cache-операции одним `synchronized` монитором; синхронизация оставлена SQLite-слою.
- SQLite trim больше не выполняется на каждую запись: добавлен throttled trim примерно раз в 5 минут на таблицу плюс ручной `trimNow()`.
- `todayScheduleCount()` получил короткий in-memory cache на день, чтобы не перечитывать и не парсить весь schedule JSON при частых вызовах.
- HTTP memory-cache в `ApiRepository` переведён с `Collections.synchronizedMap`/LRU-монитора на bounded `ConcurrentHashMap`.
- Stream-cache также переведён на concurrent map с безопасной очисткой старых записей.
- Длинные cache keys из method/url/body/headers заменены на стабильный SHA-256 ключ.
- Лимит in-memory raw response cache снижен с 450KB до 50KB на запись, чтобы снизить давление на GC.
- Для Android 12+ добавлен YORU launch splash через platform `windowSplashScreen*` attrs, без новой библиотеки и без изменения основных экранов.
- UI/функции не урезались: каталог, календарь, коллекция, загрузки, плеер, реальные озвучки и фоновые уведомления сохранены.

# 4.12.9

- Версия Android-пакета поднята до `versionName 4.12.9`, `versionCode 52`.
- Общие executors YORU переведены с крупных фиксированных пулов на bounded `ThreadPoolExecutor` с ограниченными очередями и core-timeout: меньше thread storm, меньше конкуренция за CPU/память.
- Сетевые timeout'ы каталогов, расписания, избранного и Yummy episode API снижены, чтобы плохой маршрут не держал UI и фоновые задачи слишком долго.
- Временные параллельные запросы в `ApiRepository` ограничены меньшими пулами и более короткими бюджетами ожидания.
- `ImageLoader` получил bounded queue, быстрые image timeout'ы и дедупликацию in-flight URL: одинаковый постер скачивается один раз и затем обновляет все ожидающие карточки.
- Поиск/каталог теперь агрессивнее отменяет устаревшие reset-load задачи и проверяет interruption до/после API-вызова.
- Debounce поиска увеличен, чтобы набор текста не запускал лишние тяжёлые каталожные запросы на каждую букву.
- `YoruBrain` кэширует короткоживущую статистику и использует score-map/HashSet вместо повторных дорогих расчётов в сортировках и genre-match.
- `MainActivity`, `ImageLoader` и `PlayerActivity` получили lifecycle cleanup/null-safety: меньше висящих callbacks/futures и меньше риск краша при закрытии/повороте/уходе из плеера.
- Синхронизация `SecureStore` сохранена как correctness-sensitive; вместо опасного удаления lock'ов снижены повторные горячие вычисления вокруг неё.
- Фоновая схема уведомлений 4.12.8 сохранена: JobScheduler + AlarmManager fallback, автономная проверка и отсутствие тяжёлого `checkNow()` на каждый вход.

# 4.12.8

- Фоновые уведомления о новых сериях переведены на JobScheduler + AlarmManager fallback.
- Добавлен `YoruUpdateJobService` с сетевым constraint, persisted job и retry/backoff.
- Проверка избранного теперь автономна и не зависит от открытия MainActivity.
- Убрана тяжёлая `checkNow()` при каждом возврате в приложение; старт/возврат только планируют фон.
- Профиль показывает время последней проверки, количество проверенных тайтлов и уведомлений.
- Постеры уведомлений грузятся легче и быстрее, чтобы не задерживать alert.

# YORU 4.12.3 — turbo UI, мгновенные детали и все сезоны Shikimori

- Версия Android-пакета поднята до `versionName 4.12.3`, `versionCode 46`.
- Переключение фильтров календаря переписано на готовые buckets `filter -> day -> events`: `Избранное`, `Все`, `Скоро`, `Новые для меня` больше не обходят весь список и не читают хранилище при каждом тапе.
- `MainActivity` кэширует `CalendarScreen`: возврат на вкладку календаря не пересоздаёт экран и не запускает новый первичный network/render цикл.
- `DetailsActivity.render()` больше не читает `DownloadManager` и не ходит в защищённое хранилище на каждую строку серии; прогресс/избранное/bucket читаются один раз, офлайн-статусы приходят отдельным быстрым обновлением.
- Карточка аниме загружается двумя этапами: быстрый `quickDetails()` сразу отдаёт описание/год/статус/постер, а тяжёлые серии/маршруты/полные связи приходят потом.
- Добавлен Shikimori franchise lookup: связанные аниме теперь собираются не только из direct related, но и из `/api/animes/{id}/franchise`, затем обогащаются batch GraphQL.
- Лимит отображения связанных сезонов в UI снят: показывается весь список, который YORU получил от Shikimori/franchise.
- Hidden discovery переведён в turbo-профиль: безопасные маршруты больше не режутся из-за mobile/data-saver, используется весь допустимый набор без возврата запрещённых источников.
- Общий IO-пул расширен до 8–12 потоков (`yoru-io-turbo`), чтобы не душить современные Android-устройства.
- Фоновое скачивание серий не менялось; C++/NDK в релиз не добавлялись.

# YORU 4.12.2 — ускорение календаря, избранного и списка серий

- Версия Android-пакета поднята до `versionName 4.12.2`, `versionCode 45`.
- Календарь теперь сначала мгновенно показывает сохранённое расписание, а свежие даты обновляет без блокирующего UI.
- Фильтр `Избранное` ускорен: `SecureStore` строит in-memory индексы по `source:id`, MAL/Shikimori, Anilist, KP и нормализованным названиям, поэтому `favorite()` и `progress()` больше не сканируют весь JSON на каждый item.
- Подмешивание расписания избранных переписано: сначала batch по известным ID, затем короткий параллельный дозапрос только для старых записей без ID. Последовательные `details()` по каждому избранному удалены из календарного пути.
- Memory-cache сетевых ответов расширен и получил более длинный TTL для расписания/метаданных, чтобы повторный вход в календарь не создавал лишние запросы.
- Общий IO-пул стал адаптивным по числу ядер, с именованными worker-потоками и меньшей конкуренцией за UI.
- Экран деталей больше не проверяет DownloadManager отдельно для каждой серии: скачанные серии собираются одним проходом на весь render.
- Future-заглушки для длинных онгоингов ограничены ближайшими 24 будущими эпизодами, но короткие сезоны вроде `15 из 25` по-прежнему показывают все будущие серии 16–25 с датами/уточнением.
- Кнопка скачивания на карточке открывает выбор серии сразу из уже загруженного списка, без окна `Загружаем список серий…` и без формулировки `в фоне`.
- Фоновое скачивание серий, Wi‑Fi-only, фильтры озвучек/качеств и hidden fallback выбранной озвучки не менялись.
- Сохранены исправления 4.12.1: будущие серии не выглядят playable, календарь узнаёт избранное между источниками, уведомления сравнивают вышедшие серии.

# YORU 4.12.1 — исправление календаря избранного и future-серий

- Hotfix после 4.12.0: будущие серии больше не остаются обычными строками с подписью `Видео-превью`, если источник заранее отдал технические строки 16–25, а расписание говорит, что вышло только 15.
- `appendFutureEpisodes()` теперь доверяет `episodesAired`: все серии с номером выше уже вышедшего количества принудительно становятся future-заглушками, очищаются от потоков/вариантов/постеров и показывают дату выхода.
- Экран деталей дополнительно сам проверяет `номер серии > episodesAired`, поэтому даже при кривых данных источника показывает `Выйдет дата` или `Дата уточняется`, а не обычное превью.
- `applyEpisodeVisuals()` больше не подставляет постер/скриншот future-сериям.
- Календарь теперь отдельно подмешивает расписание избранных тайтлов, а не зависит только от общего топа онгоингов.
- Фильтр календаря `Избранное` снова узнаёт сохранённые аниме между `YORU`, `Shikimori`, `AniLibria`, `AniLib` и другими карточками через MAL/Shikimori/Anilist/KP ID или нормализованное название+год.
- Для старых избранных без ID календарь пытается найти Shikimori-карточку по названию и подтянуть `nextEpisodeAt`.
- Календарь показывает 21 день, как заявлено, и при выборе фильтра автоматически переходит на ближайший день с событием.
- История просмотра получила такой же identity-match, чтобы `Новые для меня` не ломалось из-за смены source-id.
- Уведомления о новых сериях сравнивают уже вышедшее число серий, а не плановый total; старые записи с завышенным `episodesSeen` ремонтируются.
- Версия Android-пакета поднята до `versionName 4.12.1`, `versionCode 44`.

# YORU 4.12.0 — серии, расписание и умный fallback озвучек

- Версия основного Android-пакета поднята до `versionName 4.12.0`, `versionCode 43`.
- Карточки YORU теперь показывают статус тайтла: `Онгоинг`, `Закончен`, `Анонс` или уточнение статуса.
- На карточках и в деталях добавлена строка прогресса релиза: `Вышло X из Y`, если источник отдаёт общее и уже вышедшее количество серий.
- В модель добавлены `episodesAired` и `nextEpisodeAt`; они сохраняются в кэше/оффлайн-метаданных и переносятся между скрытыми источниками.
- YORU-каталог берёт быстрый слой метаданных Shikimori, чтобы сразу иметь total/airing/status/nextEpisodeAt, а просмотр по-прежнему открывает скрытые рабочие маршруты.
- Экран тайтла обогащает карточку индивидуальным расписанием Shikimori `nextEpisodeAt`, когда доступен MAL/Shikimori ID.
- Если вышло, например, `15 из 25`, будущие серии добавляются как непроигрываемые заглушки: без обычной обложки/кнопки play, с датой следующей серии или `Дата уточняется`.
- Нажатие на будущую серию или её кнопку загрузки больше не пытается открыть плеер и показывает дату выхода.
- Плеер и загрузки пропускают future-серии, чтобы не писать ложное `серия недоступна` для ещё не вышедшего эпизода.
- Выбранная/дефолтная озвучка теперь является быстрым предпочтением, а не финальным фильтром: если AniLibria.TV или другой голос отсутствует, YORU автоматически раскрывает доступные реальные озвучки.
- `loadYoruEpisode`, группировка озвучек в плеере, rescue-playback и download-options получили fallback без строгой привязки к отсутствующему голосу.
- Скрытый source discovery оставляет выбранный голос первым, но не отрезает остальные маршруты: YORU проверяет больше hidden-источников только когда это нужно.
- Anix/Sekai route больше не фильтрует всё насмерть по выбранной озвучке: сначала сортирует нужный голос выше, затем подхватывает доступные варианты.
- Пользовательские подписи `YORU Prime`, `YORU Reserve`, `YORU Max`, Yummy/Kodik/Anix route-бренды очищены до простого `YORU`; озвучкой остаётся только реальная команда перевода.
- Корневые неиспользуемые Kotlin Gradle-плагины удалены; активная сборка остаётся Java-only `:app`.

# YORU 4.11.0 — возврат к стабильному Java и экономный плеер

- Продолжение сделано от последней Java-базы 4.10.0, не от старого 2.0.1.
- Kotlin-модуль удалён из проекта, Gradle включает только `:app`, workflow снова собирает Java release APK.
- Версия основного Android-пакета поднята до `versionName 4.11.0`, `versionCode 42`.
- Из пользовательских текстов убраны технические `YORU Source` и `YORU Auto`: интерфейс показывает простой YORU.
- Нормализована реальная озвучка `AniDUB`; внутренние YORU/Yummy/Kodik/Anix маршруты остаются скрытыми.
- Выбранная реальная озвучка теперь считается строгим быстрым путём: серия открывается без повторного вопроса, а поиск не гоняет лишние голоса.
- Автоподбор маршрутов стал voice-aware: AniDUB/AniLibria.TV/AnimeVost/AniMedia сначала идут через наиболее вероятные рабочие источники.
- На мобильной/слабой сети уменьшается число фоновых маршрутов, а прогрев следующей серии отключён без конкретной выбранной озвучки.
- В нативный YORU Player добавлена линия прогресса с ручной перемоткой поверх видео.
- Скачивание стало понятнее: варианты показывают озвучку, качество и примерный размер, а Wi‑Fi-only текст не раскрывает внутренние маршруты.

# YORU 4.10.0 — последняя Java-база перед 4.11.0

- Основной Java YORU получил круглый индикатор загрузки в плеере без длинных надписей поверх видео.
- Версия основного Android-пакета: `versionName 4.10.0`, `versionCode 41`.
- Поздний Kotlin-эксперимент больше не является частью активного продукта и удалён в 4.11.0.

# YORU 4.9.0 — быстрый YORU Player и умное заполнение экрана

- Ускорен старт видео: выбранная озвучка предварительно резолвится ещё до возврата в UI, поэтому плеер чаще получает готовые потоки без второго круга ожидания.
- Уменьшены буферы старта YORU Player: видео начинает играть быстрее, без ожидания большого запаса.
- Сетевые таймауты для каталогов и видеопотоков снижены, чтобы не висеть на плохом маршруте слишком долго.
- Видеокэш теперь допускает безопасные редиректы потоков и использует более быстрый профиль ожидания.
- Прогрев следующей серии теперь заранее резолвит выбранную озвучку, если сеть не в режиме экономии.
- Старая надпись подготовки убрана из плеера.
- Загрузка видео была переработана визуально; в 4.10.0 текст из overlay полностью убран.
- Кнопки плеера перерисованы в glass-стиле с тенью, мягким контуром и акцентной кнопкой play/pause.
- Добавлен центральный красивый индикатор быстрой перемотки `+10` / `−10` вместо старого Toast.
- Кнопка скорости стала заметнее, а быстрые режимы расширены до `2.5×`.
- Добавлена отдельная акцентная кнопка `Умно расширить экран`: она открывает fullscreen и включает заполнение кадра без растягивания лиц.
- Повторное нажатие на умное расширение возвращает полный кадр без обрезки.
- Старый ручной переключатель масштаба сохранён как быстрый fallback.
- Добавлены зеркала Shikimori GraphQL и REST: `shikimori.io`, `shikimori.one`, `shikimori.me`.
- Скриншоты, трейлеры, связанные аниме и расписание становятся устойчивее, если один домен Shikimori временно недоступен.
- Kage/ReAnime/AniLibria torrent-направления изучены; в код добавлены только безопасные части, которые не замедляют просмотр и не тащат лишний трафик.
- Версия Android-пакета поднята до `versionName 4.9.0`, `versionCode 40`.

# YORU 4.8.0 — полный список серий и переработка карточки

- Удалён неудачный эксперимент с короткой вертикальной лентой: кода, activity, manifest-записи и входа на главном экране больше нет.
- Карточка аниме теперь показывает полный список серий, без ограничения на первые 80.
- Порядок карточки переработан: описание, скриншоты, трейлер, просмотр, затем серии.
- Лишнее дополнительное меню убрано из карточки вместе со связанными действиями.
- Настройки просмотра вынесены в явный блок: озвучка и разрешение по умолчанию.
- Стартовая кнопка показывает и озвучку, и разрешение.
- Добавлено разрешение `360p` для просмотра и фильтров загрузки.
- Если озвучка уже выбрана, повторный вопрос перед просмотром не появляется.
- Если озвучки ещё нет, первый диалог позволяет выбрать и озвучку, и разрешение.
- Скриншоты открываются в полный экран.
- В fullscreen-просмотре скриншотов можно приближать и отдалять двумя пальцами.
- В fullscreen-просмотре скриншотов можно двигать увеличенное изображение пальцем.
- Связанные аниме теперь добираются через Shikimori для карточек любых источников, если найден MAL/Shikimori ID или совпадение по названию.
- Связанные тайтлы, фильмы, OVA и спецвыпуски сортируются по дате/году выхода.
- Блок связанных аниме расширен до 36 элементов.
- В связанных тайтлах показывается дата выхода, если она доступна.
- Плеер получил более крупные overlay-кнопки.
- Иконки в интерфейсе и нижней панели стали крупнее.
- Видеокарточка плеера получила более аккуратные отступы, радиус и тень.
- Панель выбора серии/озвучки/разрешения стала крупнее и читабельнее.
- Блок следующих серий в плеере получил мини-превью.
- Документация и handoff обновлены под новую структуру без удалённой ленты.
- Версия Android-пакета поднята до `versionName 4.8.0`, `versionCode 39`.


# YORU 4.7.0 — превью серий и визуальная карточка

- Если пользователь уже выбрал озвучку, серия открывается сразу без повторного диалога.
- При повторном запуске серии учитывается последняя выбранная озвучка этой серии.
- Экран серии больше не заставляет каждый раз подтверждать уже выбранную озвучку.
- В плеере убрана пустая надпись `Устройтесь поудобнее`.
- На её месте появился блок следующих серий этого же аниме.
- Следующие серии в плеере кликабельны и открываются без возврата в карточку.
- Список следующих серий обновляется при переключении серии.
- В списке серий добавлены визуальные превью/обложки видео.
- Для серии используются её собственные `preview/screenshot/image/poster`, если источник отдаёт картинку.
- Если у серии нет отдельной картинки, YORU берёт кадр из скриншотов тайтла.
- Если кадров нет, остаётся обычный постер, чтобы список не был пустым.
- Карточка аниме получила блок `Кадры и трейлер`.
- Скриншоты подтягиваются через Shikimori REST `/screenshots`.
- Трейлер подтягивается через Shikimori REST `/videos`, с fallback на поиск трейлера.
- Визуальные данные тихо кэшируются и не ломают карточку, если внешний API временно не ответил.
- Добавлена поддержка визуальных полей Anix/Sekai, AniLib, AniLibria и Yummy.
- Версия Android-пакета поднята до `versionName 4.7.0`, `versionCode 38`.

# YORU 4.6.0 — выбранная озвучка без лишнего трафика и новый резерв

- Плеер больше не резолвит пачку чужих вариантов перед стартом серии.
- Если выбрана конкретная озвучка, в плеер попадает только она и максимум несколько её резервных вариантов.
- Поток открывается строго по одному активному варианту; остальные не подключаются заранее.
- Аварийное восстановление просмотра теперь остаётся внутри текущей озвучки и не прыгает на другой перевод.
- Переключение озвучки прямо в плеере сохраняет её как текущий строгий выбор.
- Предзагрузка следующей серии больше не раскрывает лишние потоки при строгом выборе.
- Список озвучек остаётся реальным: берётся только название команды/перевода, не технический маршрут.
- Добавлен скрытый резервный маршрут YORU Reserve на базе открытого Anix/Sekai mirror API.
- Поддержаны зеркала `api-s.anixsekai.com` и `api.anixsekai.com` без авторизации и без пользовательских токенов.
- Для нового маршрута используются релиз, список типов, source внутри типа и эпизоды с URL.
- Название технического source внутри нового API не превращается в пользовательскую озвучку.
- При строгом выборе новый маршрут загружает только совпадающие типы, а не все варианты подряд.
- В авто-режиме новый маршрут ограничен по количеству типов, чтобы не тормозить YORU Source.
- Поиск нового резерва работает через обычные названия тайтла и сопоставление с уже найденной карточкой.
- Данные нового резерва нормализуют постер, год, статус, рейтинг, возраст, количество серий и жанры.
- Автоматическое обнаружение маршрутов останавливается раньше, когда уже найдено достаточно рабочих вариантов.
- Усилен HLS extraction для плеерных ссылок: поддержан `videoId` из HTML.
- Добавлен запасной JSON-вызов `/ftor`, если формовый вызов не вернул ссылки.
- Качества из HLS по-прежнему нормализуются как `360p/480p/720p/1080p`.
- Скачивание получает те же ограничения выбранной озвучки и не ищет чужие варианты без необходимости.
- Снижено лишнее сетевое потребление при открытии серии, смене серии и fallback.
- Версия Android-пакета поднята до `versionName 4.6.0`, `versionCode 37`.

# YORU Forge 0.3.0 — ARM64-only toolchain и self-test

- Forge закреплён как маленький APK до 1 МБ без встроенного SDK.
- Локальная сборка переведена на ARM64-only toolchain pack: `aapt2-arm64-v8a`, `android.jar`, dex-версии ECJ/D8/apksigner, debug-keystore и jar-зависимости.
- Добавлен self-test toolchain при импорте и перед сборкой: Forge реально запускает `aapt2`, ECJ, D8 и apksigner.
- Добавлена подготовка manifest перед сборкой: package/applicationId и `${applicationId}` больше не ломают локальный `aapt2 link`.
- Добавлена поддержка нескольких `classes*.dex`, чтобы крупные зависимости не ломали APK.
- Команды сборки запускаются с `LD_LIBRARY_PATH`, `PATH` и `TMPDIR` из приватной папки Forge для Android ARM64 toolchain.
- Добавлена спецификация `FORGE_TOOLCHAIN_PACK.ru.md` с точной структурой pack и весом.

# YORU Forge 0.2.0 — оффлайн локальный сборщик

- Forge переведён на локальную сборку без облачного workflow внутри приложения.
- Убрана зависимость интерфейса Forge от GitHub-кнопок: основной путь теперь `папка исходников → toolchain pack → локальный APK`.
- Добавлен импорт минимального toolchain pack из zip.
- Добавлена проверка нужных файлов: `android.jar`, Android-совместимый `aapt2`, dex-версии `ECJ`, `D8`, `apksig`, debug-keystore и jar-зависимости.
- Добавлен локальный build pipeline: копирование проекта из SAF, сборка ресурсов через `aapt2`, генерация `BuildConfig`, компиляция Java через ECJ, dex через D8, упаковка `classes.dex`, подпись через apksig.
- Добавлен экран лога локальной сборки.
- Forge `targetSdk` снижен до 28, чтобы устройства Android разрешали запуск импортированных локальных toolchain-бинарников из приватной папки приложения.
- Вес Forge остаётся маленьким: toolchain не встроен в APK, а импортируется отдельно.

# YORU Forge 0.1.0 — отдельный редактор и сборщик

- Добавлен отдельный Android-модуль `:forge` с приложением `YORU Forge`.
- Forge умеет выбирать папку исходников через системный проводник Android, читать дерево проекта и открывать текстовые файлы.
- Добавлен встроенный редактор с сохранением `.java`, `.gradle`, `.xml`, `.json`, `.md`, `.properties`, `.yml` и других текстовых файлов.
- Добавлен экспорт исходников в zip без `.git`, build-артефактов, APK, архивов и signing-файлов.
- Добавлены кнопки открытия GitHub Actions для release-сборки YORU и Forge, а также ссылка на папку готовых APK.
- Добавлен отдельный workflow `.github/workflows/build-forge-apk.yml`, который собирает только release APK Forge.
- Основной workflow YORU ограничен путями приложения, чтобы правки Forge не пересобирали YORU без причины.

# YORU 4.5.2 — озвучка видна до просмотра

- На экране тайтла явно показана озвучка по умолчанию: `Авто` или выбранная реальная команда.
- Кнопка просмотра больше не стартует серию вслепую: перед запуском открывается выбор доступных озвучек именно этой серии.
- Кнопки скачивания на экране тайтла и у каждой серии показывают выбранную озвучку или честный текст `выбор озвучки`.
- В плеере кнопка скачивания показывает текущую озвучку выбранного варианта.
- В списке вариантов скачивания неизвестный голос больше не пропадает: он подписан как `Дорожка без названия`, без подстановки технического маршрута.
- Название серии больше не используется как озвучка.
- Версия Android-пакета поднята до `versionName 4.5.2`, `versionCode 36`.

# YORU 4.5.1 — корректные озвучки и handoff-пакет

- Исправлена ошибка 4.5.0: внутренние маршруты больше не считаются озвучками и не попадают в выбор голоса.
- Список озвучек в плеере строится по реальным названиям, доступным именно у выбранного аниме и серии.
- Поддержаны реальные голоса в фильтрах: AniDUB, AniLibria.TV, AniMaunt, AnimeVost, AniStar & DEEP, Beyond:Studio, Dream Cast, AniMedia, StudioBand, JAM, SHIZA Project и другие названия, если они приходят у конкретной серии.
- Если у варианта нет названия голоса, YORU не придумывает брендированную озвучку и показывает нейтральную дорожку без названия только как запасной вариант.
- Скачивание сначала загружает доступные озвучки конкретной серии, затем применяет фильтр голоса и разрешения.
- Офлайн-просмотр больше не сохраняет технический маршрут как любимую озвучку.
- В репозиторий добавлен zip-пакет с исходниками, инструкцией сборки APK и публичным гайдом для будущего агента.
- Версия Android-пакета поднята до `versionName 4.5.1`, `versionCode 35`.

# YORU 4.5.0 — озвучки, быстрые загрузки и единый YORU Player

- Интерфейс просмотра переведён на озвучки: пользователь выбирает голос, а не технический маршрут.
- Убран старый пользовательский выбор вариантов просмотра по каталогам; карточка открывается через YORU Player и YORU Source.
- YORU Source объединяет найденные серии в один список и не дублирует одинаковые озвучки в интерфейсе.
- Рабочие маршруты используются как скрытый резерв, когда нужная озвучка отсутствует или текущий маршрут не открылся.
- Добавлена настройка «Показывать только выбранную озвучку»: при выборе AniDUB, AniLibria.TV, AniMaunt, AnimeVost, AniStar & DEEP, Beyond:Studio, Dream Cast, AniMedia или субтитров плеер и загрузки скрывают остальные голоса.
- Скачивание стало быстрее: перед поиском выбираются озвучка и разрешение, а YORU не тратит время на лишний полный перебор.
- Массовое скачивание получило те же фильтры озвучки и разрешения.
- Повтор неудачной загрузки ищет ту же озвучку и качество через актуальный рабочий маршрут, без привязки к старому внутреннему пути.
- Нативный плеер остаётся единственным пользовательским просмотром: при сбое YORU подбирает другой рабочий вариант внутри YORU Player.
- Обновлены README, changelog, workflow сборки и версия Android-пакета до `versionName 4.5.0`, `versionCode 34`.

# YORU 4.4.1 — UX-исправления плеера, загрузок и настроек

- Убран дублирующий стандартный контроллер видеоплеера: теперь поверх видео остаётся один слой кнопок YORU.
- Настройка скачивания снова показывает конкретные разрешения: 480p, 720p, 1080p.
- В настройках и загрузках оставлена формулировка только про Wi‑Fi.
- Главный экран и настройки сохраняют позицию прокрутки при выходе из приложения, переходе в другое приложение и возврате.
- Перед скачиванием и в плеере показывается быстрый ориентир размера серии по выбранному разрешению.
- Обновлены README, changelog, workflow сборки и версия Android-пакета до `versionName 4.4.1`, `versionCode 33`.

# YORU 4.4.0 — большой релиз стабильности, интерфейса и плеера

- Полностью удалены два нерабочих направления из каталогов, автоподбора, просмотра, загрузок, изображений и документации.
- Нижняя навигация сокращена до пяти основных разделов; настройки остаются сверху и не занимают постоянную вкладку.
- Удалено откладывание уведомлений по расписанию: новые серии больше не блокируются этим фильтром.
- Удалён лишний безопасный переключатель из интерфейса, импорта и логики подбора.
- Проверка новых серий стала активнее: при добавлении в коллекцию, запуске YORU, возврате в приложение и частом фоновом цикле.
- Уведомление о новой серии получило быстрое действие «Смотреть».
- Список избранного для проверки расширен, чтобы больше тайтлов успевали обновляться за один цикл.
- Профиль переписан как компактные настройки: новые серии, сеть, стабильность загрузок, комфорт, память и приложение.
- Главный экран получил быстрый старт: продолжить просмотр, случайный тайтл, проверка новых серий и переход к загрузкам.
- Нативный плеер стал компактнее: предыдущая/следующая серия теперь внутри видео, больших кнопок под видео больше нет.
- Добавлена сохраняемая скорость просмотра.
- Добавлены жесты яркости и громкости в плеере.
- Улучшено возобновление: почти досмотренная серия не открывается заново на последних секундах.
- YORU Source быстрее отбрасывает слабые варианты, сильнее учитывает успешные запуски и меньше ждёт пустые направления.
- Увеличено полезное кеширование карточек и вариантов, чтобы повторное открытие работало быстрее.
- Редкие технические подписи вариантов скрываются за понятным запуском YORU.
- Долгое нажатие на серию в карточке отмечает её просмотренной.
- Обновлены README, changelog, workflow сборки и версия Android-пакета до `versionName 4.4.0`, `versionCode 32`.

# YORU 4.3.0 — обложки и устойчивость карточек

- Исправлены обложки и превью AniLib / AniLib Ultra: YORU выбирает лучший доступный вариант изображения и повторяет загрузку через более устойчивый профиль подключения.
- Общий режим оставлен быстрым: тяжёлые направления не запускаются лишним фоном.
- Скрытая адаптивная система ранжирования стала лучше отодвигать слабые варианты после неудач.
- Обновлены README, changelog, workflow сборки и версия Android-пакета до `versionName 4.3.0`, `versionCode 31`.

# YORU 4.2.0 — чистый мощный релиз

- Удалён нерабочий каталог из пользовательских списков, автоподбора, просмотра, скачивания и профиля устойчивости.
- Убраны лишние отдельные входы и дублирующие разделы, которые отвлекали от поиска, карточек и просмотра.
- YORU Source теперь работает сильнее под капотом: ранжирует доступные направления, запоминает удачные запуски и отодвигает слабые варианты без отдельной пользовательской панели.
- Добавлена тихая подготовка следующей серии во время просмотра: когда серия уже идёт и сеть не в экономии, YORU заранее подготавливает продолжение.
- Улучшен поиск рабочих вариантов: приложение пробует точное название, оригинальное название и очищенные варианты без сезонного/рекламного шума.
- Карточка серий стала сильнее: текущая серия выделяется, а быстрый пакет «Скачать дальше» запускает массовое скачивание прямо из списка серий.
- Массовое скачивание использует уже загруженный список серий, если карточка его получила, и не гоняет каталог повторно без необходимости.
- Автоспасение просмотра сохранено: при сбое YORU пробует другое качество или следующий подходящий вариант без лишней навигации.
- Общий каталог остался быстрым: сомнительные или тяжёлые направления не запускаются в фоне.
- Профиль и настройки стали короче: остались только просмотр, внешний вид, данные и информация о проекте.
- Обновлены README, changelog, workflow сборки и версия Android-пакета до `versionName 4.2.0`, `versionCode 30`.

# YORU 4.1.0

- Предыдущий релиз усиливал ядро YORU, но один из добавленных маршрутов оказался нерабочим и удалён в 4.2.0.

# YORU 4.0.0

- Предыдущий релиз добавлял адаптивный порядок маршрутов, объединение дублей, автоспасение просмотра и более строгий выбор рабочих вариантов. В 4.2.0 эти возможности сохранены как скрытая часть YORU Source без отдельного пользовательского раздела.

# YORU 3.8.0

- Интерфейс был уплотнён, профиль стал короче, карточка аниме стала легче, а сильные агрегаторы получили закрытые названия YORU.

# YORU 3.7.0

- Добавлены приватный режим, режим без спойлеров, умные подборки, оценки, теги, закрепления, скрытие тайтлов, улучшения календаря и управления загрузками.

# YORU 3.6.0


# YORU 3.5.0

- Добавлен отдельный календарь выхода, расширены настройки просмотра и улучшен запуск серий из карточки.
