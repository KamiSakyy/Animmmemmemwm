# YORU Android Java 4.12.7 — актуальное состояние

Дата: 2026-09-06. Ветка: `arena/01a07147-animmmemmemwm`.

## Активное направление

Текущий продукт — стабильное Java-приложение `app.yoru.mobile` в `yoru-android/app`. Kotlin не является активным направлением: активная сборка включает только `:app`. Forge/toolchain в релизе 4.12.7 не используется.

## Итог 4.12.7

- Версия: `versionName 4.12.7`, `versionCode 50`.
- Release APK: `apk-output/YORU-4.12.7-release.apk`.
- APK SHA256: `d898d9265998d503f67d4530420db91c4812f1a2a7dc3734f1c04e717e5231ec`.
- Успешный GitHub Actions run: `34050519015`.
- Source commits: `95cc581` (`Restore full calendar feed and download previews`) и `44004aa` (`Fix home hero variable scope`).
- APK commit: `27d13d7` (`Add built YORU 4.12.7 release APK [skip ci]`).

## Срочно исправлено после 4.12.6

- `CalendarScreen` переписан с `ScrollView + nested RecyclerView` на один полноэкранный `RecyclerView`.
- Шапка, hero, дни, фильтры и события календаря теперь элементы одного списка, поэтому весь поток событий прокручивается нормально и не обрезается до 2 карточек.
- Календарь по умолчанию стартует в режиме `Все` (`selected = -1`) и показывает события всех дней за период, а не только один выбранный день.
- Горизонтальные чипы дней остались только ручными фильтрами после нажатия пользователя.
- Период календаря расширен до 28 дней; warm-cache тоже запрашивает 28 дней.
- Расписание стало шире: больше страниц ongoing/anons и fallback до 120 тайтлов; для тайтлов без точной даты добавляются аккуратные слоты `Скоро`, чтобы лента не схлопывалась до пары событий.
- Видимые системные scrollbars скрыты в календаре, главной, деталях, плеере, списках и кастомных диалогах, где они выглядели как грубая полоса.
- Главная восстановлена как красивая anime hero/poster-карточка с Фрирен; технический текст `Смотрите без лишних экранов` и подобные app-change формулировки убраны.
- Загрузки получили крупную preview/poster-карточку для скачанных эпизодов с затемнением, подписью серии и play-иконкой; фоновая download-служба не ломалась.
- Нижняя навигация по-прежнему показывает строго `Календарь` без цифр/букв/бейджей.
- Стартовый loading/shell экран и тексты `Открываем…` не возвращались.

## Сохранено из 4.12.6/4.12.5/4.12.4

- `MainActivity.onCreate()` вызывает настоящий `render()` сразу, без промежуточной заставки.
- Главная строит первые блоки без обязательного синхронного чтения seed/store; рекомендации подставляются фоном.
- `YoruCache.java`: SQLite fast-cache для `details`, `schedule`, `franchise`, `offline`, `progress` с TTL/лимитами/trim.
- Screen-cache для `Главная`/`Каталог`/`Коллекция`/`Календарь`.
- `DetailsActivity` использует `RecyclerView` для списка серий и отменяет старые detail/download задачи.
- `DownloadHub` имеет fast offline-index, чтобы UI не сканировал `DownloadIndex` на каждую строку серии.
- Франшиза: SQLite-cache, stable sort, фильтры `Все/Сезоны/Фильмы/OVA/Спешлы`, кнопка `Смотреть по порядку франшизу`.
- Hidden discovery использует максимум маршрутов в рамках дедлайна, без раннего stop по количеству найденных вариантов.
- Плеер не режет качество/буфер под mobile/data-saver default и заранее pre-check следующей серии.
- `SecureStore`, `ApiRepository`, `TrafficMeter` остаются lazy, чтобы не держать холодный старт.

## Проверки

- `git diff --check`: OK перед коммитом 4.12.7.
- Локальная Gradle-сборка в Arena невозможна из-за отсутствия `java/JAVA_HOME`.
- GitHub Actions `34050519015`: success, шаг `Build Java release APK` прошёл.
- `sha256sum apk-output/YORU-4.12.7-release.apk`: `d898d9265998d503f67d4530420db91c4812f1a2a7dc3734f1c04e717e5231ec`.

## Жёсткие правила будущего продолжения

- Работать только с Java Android `app.yoru.mobile`, пока пользователь явно не поменяет направление.
- Не возвращать Kotlin, Forge/toolchain, Clips, “Ещё”, AniList как пользовательский источник, TSM, AnimeGO, JutSu, SameBand, SovetRomantica, Yummy Legacy.
- Не добавлять VPN/VLESS/Xray/proxy/private tokens/API keys и MP4-конвертацию.
- Не возвращать стартовый loading/shell экран и startup-заглушки.
- Не добавлять цифры/бейджи в нижнюю кнопку `Календарь`.
- Календарь по умолчанию должен показывать весь список событий, не один выбранный день; не возвращать nested-scroll layout, который снова может показать только 2 карточки.
- Не показывать грубые системные scrollbars, если пользователь явно не попросил обратное.
- “Озвучка” — только реальная команда перевода/дубляжа; технический маршрут не должен называться озвучкой.
- Выбранная/дефолтная озвучка — предпочтение для скорости, но отсутствие этой озвучки не должно ломать playback/download.
- Будущие серии должны быть визуально непроигрываемыми и показывать дату выхода/уточнение, а не обычную обложку.
- Календарь избранного должен оставаться быстрым: не возвращать repeated JSON scans, repeated filter scans и последовательные сетевые details по каждому избранному.
- Cache должен оставаться bounded: TTL, лимиты, trim, без бесконечного stale-cache и без лишнего расхода интернета.
